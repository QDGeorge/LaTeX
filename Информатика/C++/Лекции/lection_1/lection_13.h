Интерфейс функции включает:
Имя, смысл и типы параметров, допустимые значения параметров, возможные исключения (exeption).
try {
    do();
    something();
    dangerous();
}
catch (very specEx &x) {
std::cout << "У нас супер-особый случай";
}
else if (specialEx &x) {
if (x == msg...) {...}
else throw;
}